node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
.buron/

# Local agent / IDE (skills and config live in repo source, not here)
.claude/
.cursor/
.obsidian/
CLAUDE.md
---
# Keep lockfile out of the repo — biome's optionalDependencies need to
# resolve at install time per platform, and a macOS-generated lockfile
# breaks the Linux CI runner's biome install.
package-lock.json
